  <div class="container">
    <div id="titlebox">
      <div id="titleElement">
        <div class="row hihTitle">
          <div class="col s12">
            <h1 class="text">Hi, Human</h1>
          </div>
        </div>
      </div>
    </div>
  {{#if isLoggedIn}}
    <div class="row button-row">
      <div class="col s12 button-element">
          <a href="/user" class="row button modal-trigger hihTitle waves-effect waves-light btn-large">
            {{ buttonData }}
          </a>
      </div>
    </div>
  {{else}}
    <div class="row button-row">
      <div class="col s12 button-element">
          <a href="/login" class="row button modal-trigger hihTitle waves-effect waves-light btn-large">
            {{ buttonData }}
          </a>
      </div>
    </div>
  {{/if}}
  </div>
